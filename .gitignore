__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Tenant context names a subscription and describes an estate. Never commit it.
tenant/*.json
*tenant-context*.json
!tenant/example*.json

# Raw ingest cache; snapshots under snapshots/ ARE committed (they are the pinned truth).
.cache/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.auto.tfvars.json
